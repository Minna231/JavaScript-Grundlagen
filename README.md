# 🇩🇪 JavaScript Grundlagen: Deutsche Variablen und Kommentare

![Node.js](https://img.shields.io/badge/Node.js-339933?style=flat-square&logo=nodedotjs&logoColor=white)
![Sprache](https://img.shields.io/badge/Sprache-Deutsch-blue)

## 📌 Projektübersicht
Dieses **Anfänger-Projekt** dient als einfache Übung, um die grundlegende JavaScript-Syntax (Funktionen, Variablen, Bedingungen) mit **deutschen Bezeichnungen** zu verknüpfen. Es ist ideal für Programmierer, die ihre deutschen Vokabeln im Coding-Kontext üben möchten.

## 📝 Was das Skript macht (Funktionalität)
Das Skript `gruss.js` führt folgende einfache Operationen aus:
1.  Es definiert eine Funktion (`begrüsseBenutzer`), die einen Namen als Argument entgegennimmt.
2.  Es gibt eine Begrüßungsnachricht (Grußnachricht) auf der Konsole aus.
3.  Es enthält eine einfache **If/Else-Bedingung** (`if (alter > 18)`), um den rechtlichen Status (*volljährig* oder *minderjährig*) basierend auf einer Variablen zu bestimmen.

## 🔑 Gelernte Vokabeln (Wichtige Begriffe)

| JavaScript-Konzept | Deutsche Bezeichnung |
| :--- | :--- | 
| `function` | `begrüsseBenutzer` | 
| `variable` | `meinName` |
| `variable` | `alter` | 
| `if/else` | `volljährig` / `minderjährig` |

## ⚙️ Installation

Dieses Skript benötigt lediglich **Node.js** auf Ihrem System.

1.  **Repository klonen:**
    ```bash
    git clone [Ihre-Repo-URL]
    cd [Ihr-Projektname]
    ```

2.  **Ausführung:**
    Führen Sie das Skript direkt über Node.js aus:
    ```bash
    node gruss.js
    ```

## 💻 Erwartete Ausgabe (Erwarteter Output)
Wenn Sie mehr erfahren möchten, besuchen Sie https://sotdb.de/
